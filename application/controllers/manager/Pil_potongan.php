<?php

if (!defined('BASEPATH'))
    exit('No direct script access allowed');

class Pil_potongan extends CI_Controller
{
    function __construct()
    {
        parent::__construct();
        $this->load->model('Tampilan_model');
        $this->load->model('Pil_potongan_model');
        $this->load->library('form_validation');        
	    $this->load->library('datatables');
        $this->Tampilan_model->admin();
    }

    public function index()
    {
        $this->Tampilan_model->layar('pil_potongan/pil_potongan_list');
    } 
    
    public function json() {
        header('Content-Type: application/json');
        echo $this->Pil_potongan_model->json();
    }

    public function read($id) 
    {
        $row = $this->Pil_potongan_model->get_by_id($id);
        if ($row) {
            $data = array(
    		'id' => $row->id,
    		'nama_potongan' => $row->nama_potongan,
	    );
            $this->Tampilan_model->layar('pil_potongan/pil_potongan_read', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('admin/pil_potongan'));
        }
    }

    public function create() 
    {
        $data = array(
            'button' => 'Create',
            'action' => site_url('admin/pil_potongan/create_action'),
    	    'id' => set_value('id'),
    	    'nama_potongan' => set_value('nama_potongan'),
	);
        $this->Tampilan_model->layar('pil_potongan/pil_potongan_form', $data);
    }
    
    public function create_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->create();
        } else {
            $data = array(
		'nama_potongan' => $this->input->post('nama_potongan',TRUE),
	    );

            $this->Pil_potongan_model->insert($data);
            $this->session->set_flashdata('message', 'Create Record Success');
            redirect(site_url('admin/pil_potongan'));
        }
    }
    
    public function update($id) 
    {
        $row = $this->Pil_potongan_model->get_by_id($id);

        if ($row) {
            $data = array(
                'button' => 'Update',
                'action' => site_url('admin/pil_potongan/update_action'),
        		'id' => set_value('id', $row->id),
        		'nama_potongan' => set_value('nama_potongan', $row->nama_potongan),
	    );
            $this->Tampilan_model->layar('pil_potongan/pil_potongan_form', $data);
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('admin/pil_potongan'));
        }
    }
    
    public function update_action() 
    {
        $this->_rules();

        if ($this->form_validation->run() == FALSE) {
            $this->update($this->input->post('id', TRUE));
        } else {
            $data = array(
		      'nama_potongan' => $this->input->post('nama_potongan',TRUE),
	    );

            $this->Pil_potongan_model->update($this->input->post('id', TRUE), $data);
            $this->session->set_flashdata('message', 'Update Record Success');
            redirect(site_url('admin/pil_potongan'));
        }
    }
    
    public function delete($id) 
    {
        $row = $this->Pil_potongan_model->get_by_id($id);

        if ($row) {
            $this->Pil_potongan_model->delete($id);
            $this->session->set_flashdata('message', 'Delete Record Success');
            redirect(site_url('admin/pil_potongan'));
        } else {
            $this->session->set_flashdata('message', 'Record Not Found');
            redirect(site_url('admin/pil_potongan'));
        }
    }

    public function _rules() 
    {
	$this->form_validation->set_rules('nama_potongan', 'nama potongan', 'trim|required');

	$this->form_validation->set_rules('id', 'id', 'trim');
	$this->form_validation->set_error_delimiters('<span class="text-danger">', '</span>');
    }

}

/* End of file Pil_potongan.php */
/* Location: ./application/controllers/Pil_potongan.php */
/* Please DO NOT modify this information : */
/* Generated by Harviacode Codeigniter CRUD Generator 2018-09-05 06:19:28 */
/* http://harviacode.com */